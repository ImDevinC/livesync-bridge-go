{
    "_comment": "For detailed configuration documentation, see USAGE.md",
    "peers": [
        {
            "type": "couchdb",
            "name": "vault-main",
            "group": "main",
            "database": "livesync",
            "username": "admin",
            "password": "password",
            "url": "http://localhost:5984",
            "passphrase": "your-encryption-passphrase",
            "baseDir": "shared/",
            "customChunkSize": 100,
            "minimumChunkSize": 20,
            "enableCompression": true,
            "initialSync": true,
            "_comment_conflictResolution": "Optional: Strategy for resolving edit conflicts. Valid values: 'timestamp-wins' (default, newest wins), 'local-wins' (local always wins), 'remote-wins' (remote always wins), 'manual' (error on conflict). If omitted, defaults to 'timestamp-wins'.",
            "conflictResolution": "timestamp-wins"
        },
        {
            "type": "storage",
            "name": "local-vault",
            "group": "main",
            "baseDir": "./vault/",
            "scanOfflineChanges": true
        }
    ]
}
